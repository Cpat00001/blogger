{% block body %}
    <div>
        <div class="row">
            <div class="col-sm-12">
                {# {{ dump(comment) }} #}
                <br>
                 {# {{ form }}  #}
            </div>
            <div class="col-sm-12">
                {{ form(form) }}
            </div>
        </div>
        
    </div>
{% endblock %}
